# SQL Practice

Start with [SQL Practice Setup](SETUP.md). Then use the public [question catalog](questions.md), the [CSV and SQLite practice kit](../../../../../resources/sql/README.md), and the [allowed schema](../schema-registry.md). Record real answers and progress only in local `private-progress/`.
