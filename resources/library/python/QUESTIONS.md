# Python and pandas Question Catalog

- Count values matching a condition.
- Sum selected values without `sum()`.
- Find the largest value without `max()`.
- Combine filtering, counting, and list reconstruction.
- Build a frequency map with a dictionary.
- Find the first non-repeating value.
- Implement group-like aggregation with dictionaries.
- Calculate a sliding-window sum and maximum.
- Explain time and space complexity for a solution.
- Filter DataFrame rows with multiple conditions.
- Create a calculated column with missing values.
- Use `groupby().agg()` for several metrics.
- Merge two DataFrames with incomplete match coverage.
- Identify unmatched rows after a left merge.
- Fill or preserve missing values intentionally.
- Rank within groups.
- Calculate a previous-row metric within a group.
- Write a reusable metric function.
- Debug a grain or duplicate-row error after a merge.

## Python foundations

- Reverse a string without using slicing and explain the complexity.
- Check whether a string is a palindrome after normalizing punctuation and case.
- Find the second-largest distinct value with edge-case handling.
- Rotate a list by `k` positions.
- Flatten a nested list one level deep.
- Find duplicates while preserving first-seen order.
- Merge two dictionaries with a defined conflict rule.
- Parse a list of records and calculate a conditional average.
- Write a function with clear inputs, outputs, and failure behavior.
- Explain when a generator is preferable to a list.
- Debug an off-by-one error in a loop.
- Refactor repeated code into a small reusable helper.

## Data structures and algorithms

- Use a set to find the intersection of two lists.
- Find the first duplicate in a stream of values.
- Build a queue using `collections.deque` and explain why it fits.
- Group records by a key with a dictionary.
- Sort records by two keys with a deterministic tie-breaker.
- Compare an `O(n²)` solution with an `O(n)` alternative.

## pandas and data quality

- Parse dates and identify invalid or missing timestamps.
- Deduplicate records using an explicit business key.
- Reshape wide data to long data with `melt`.
- Pivot long metrics into a reporting table.
- Calculate a rolling average within each group.
- Flag outliers using a transparent rule.
- Compare two DataFrames and identify changed rows.
- Build a cohort table from signup and activity months.
- Test a data-cleaning function with edge cases.
- Explain when a pandas operation risks memory pressure and how to mitigate it.
